using ETModel;

namespace ETHotfix
{
    public static class DBHelper
    {
        //public static Session GetDBSession()
        //{
        //    NetInnerComponent netInnerComponent = Game.Scene.GetComponent<NetInnerComponent>();
        //    StartConfigComponent startConfigComponent = Game.Scene.GetComponent<StartConfigComponent>();
        //    InnerConfig innerConfig = startConfigComponent.Get(startConfigComponent.DBConfig.AppId).GetComponent<InnerConfig>();
        //    return netInnerComponent.Get(innerConfig.IPEndPoint);
        //}
    }
}
